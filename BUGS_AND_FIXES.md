# FAFLOW Bug Fixes & Parity Log

| Bug ID | Module | Files Changed | Web-Parity Checked? | Description | Verification / Test | Result |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **B-001** | Leave | `ApplyLeaveScreen.kt`, `FacultyFeatureViewModels.kt` | Yes (Web: single date with whole_day/custom periods; no multi-day batch) | Prevented client-side date range loop that caused partial duplicate leave failures on non-working days; aligned to Web single-date submission. | Unit test + Gradle test suite | PASS |
| **B-002** | Auth / RBAC | `AuthRepository.kt`, `FaflowDtos.kt`, `DomainModels.kt`, `TokenManager.kt` | Yes (Enforces product boundary: Android only allows `teacher` & `admin`+`secondary_admin`) | Added login-time client-side UX gate rejecting all non-Teacher/HOD roles with "This app is for Teachers and HODs. Please use the web portal." | Unit test + Gradle test suite | PASS |
| **B-003** | Attendance Integrity | `InstitutionalSchedule.kt` | Yes (Backend independently checks `sub_time >= start_time`) | Removed unsafe mutable `debugBypassPeriodTiming` property and secured `hasPeriodStarted` to strictly enforce start minute rules. | Unit test + Gradle test suite | PASS |
| **B-004** | Geofence / RBAC Scope Creep | `MoreScreen.kt`, `NavGraph.kt`, `Screen.kt` | Yes (Web geofences page requires `SYSTEM_ADMIN` write access) | Removed non-Teacher/HOD geofence admin screen navigation from mobile Operations Hub. | Unit test + assembleDebug | PASS |
| **B-005** | Substitution | `FacultyFeatureViewModels.kt`, `SubstitutionScreen.kt` | Yes (Web substitution is Teacher Self-Management; assigned duty has no separate "accept" endpoint) | Reconciled duty acceptance: removed ephemeral hallucinated "accept" state, aligned duty card directly to attendance session taking. | Unit test + assembleDebug | PASS |
| **B-006** | Notifications | `NotificationsScreen.kt` | Yes (Web `NotificationBell.jsx` navigates to top-level feature routes) | Verified top-level route navigation without crashing on missing entity IDs. | Unit test + assembleDebug | PASS |
| **B-007** | Announcements | `AnnouncementsScreen.kt` | Yes (Web downloads from backend dynamic URL) | Verified dynamic URL resolution from backend payload; no hardcoded `10.0.2.2` in download handler. | Unit test + assembleDebug | PASS |
| **B-008** | Biometrics / Auth Leakage | `AttendanceViewModel.kt` | Yes (Face verification bound strictly to authenticated session identity) | Removed insecure fallback defaulting to staff ID `1` when `targetStaffId` is unauthenticated or missing. | Unit test + assembleDebug | PASS |
| **B-009** | Offline Sync Resilience | `AttendanceRepository.kt` | Yes (Aligned with backend idempotency and HTTP status semantics) | Handled HTTP 409 Conflict as reconciled sync; capped max retries on 4xx client errors to prevent permanent queue poisoning. | Unit test + assembleDebug | PASS |
